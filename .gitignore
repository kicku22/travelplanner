__pycache__/
*.pyc

# Local secrets and per-machine Streamlit state (config.toml IS tracked)
.streamlit/secrets.toml

# Legacy file-based trip storage, replaced by the SQLite database
saved_trips/

# SQLite database: user accounts and their saved trips
*.db
*.db-journal
*.db-wal
